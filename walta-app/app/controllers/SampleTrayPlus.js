// Presenter shell only — the Titanium-free lib/mvvm/controllers/SampleTrayPlus
// (built by the tray's polymorphic collection) binds the add cell and routes the
// tap surface to its own add-to-sample intent. Nothing to wire here.
// See docs/patterns/screen-controllers.md.
