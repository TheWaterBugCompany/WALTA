// Presenter shell only — the Titanium-free lib/mvvm/controllers/SampleTaxaIcon
// screen controller (built by View.createComponent) binds the cell view-model and
// routes the tap surface to the cell's own intent. Nothing to wire here.
// See docs/patterns/screen-controllers.md.
