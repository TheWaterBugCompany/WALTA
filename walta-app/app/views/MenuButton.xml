<Alloy>
	<View id="button" class="large">
		<View layout="horizontal" horizontalWrap="false" > <!-- inserting another View seems to workaround layout bugs -->
			<ImageView id="icon" />
			<View id="text">
				<View  layout="vertical"> <!-- inserting another View seems to workaround layout bugs -->
					<Label id="title" class="headingText"/>
					<Label id="description" class="labelText"/>
				</View>
			</View>
		</View>
	</View>
</Alloy>
