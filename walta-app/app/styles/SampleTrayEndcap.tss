"#cell": {
  top: 0,
  left: 0
}

".trayBackground": {
  width: Ti.UI.FILL,
  height: Ti.UI.FILL
}

// The endcap image has a lip on the left; the two cells sit in the right 73%,
// stacked vertically (matches the old firstTwoTiles layout).
".endcapHoles": {
  width: "73%",
  left: "27%",
  height: Ti.UI.FILL,
  layout: "vertical"
}
