app-config.test.json contents are added to the Alloy configuration environment if --app-config=test
app-config.production.json contents are added if --app-config=production

app-config.sample.json is a template for these files containing secrets.