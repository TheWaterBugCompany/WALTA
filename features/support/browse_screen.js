'use strict';
/*class BrowseScreen extends Screen {
    def trait
      "* marked:'Browse'"
    end

    def choose_taxon( taxon )
        select(taxon)
        return page(TaxonScreen).await
    end 
}*/
