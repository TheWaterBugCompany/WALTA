'use strict';

/*class SiteDetailsScreen extends Screen {
/*     def trait
      "* marked:'Site Details'"
    end

    def fillout_site_details     
      enter_text(waterbody_name_field, "Test waterbody name")
      hide_keyboard_and_wait
      enter_text(near_by_feature_field, "Test near by feature")
      hide_keyboard_and_wait
     
      select("Next")
      page(HabitatScreen).await()
    end

    def survey_level_field
      field("Survey level.")
    end

    def waterbody_type_field
      field("Waterbody type.")
    end

    def waterbody_name_field
      field("Waterbody name.")
    end

    def near_by_feature_field
      field("Near by feature.")
    end

    def next_button
      field("Next")
    end */